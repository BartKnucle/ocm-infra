---
  - name: "Setting up gitlab"
    hosts: localhost
    tasks:

    - name: Set gitlab default projects
      gitlab_project:
        server_url: http://localhost/
        validate_certs: False
        api_username: root
        api_password: "ChangePassword!"
        name: my_first_project
        state: present
      delegate_to: localhost